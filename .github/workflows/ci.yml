name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: candy-cloud
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_DATABASE: candy_cloud_test
          MYSQL_ROOT_PASSWORD: test-root-password
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping -h 127.0.0.1 -uroot -ptest-root-password --silent"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      DATABASE_URL: mysql://root:test-root-password@127.0.0.1:3306/candy_cloud_test
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          path: candy-cloud
      - name: Require private Core read credential
        env:
          CORE_READ_TOKEN: ${{ secrets.CANDY_CORE_READ_TOKEN }}
        run: test -n "$CORE_READ_TOKEN" || { echo 'CANDY_CORE_READ_TOKEN must be configured for private candy-core checkout' >&2; exit 1; }
      - name: Check out pinned Candy Core
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          repository: reTsubasa/candy-core
          ref: 4bdd30d97852d1773bb5c60d7907bfb423e3ce1b
          path: candy-core
          token: ${{ secrets.CANDY_CORE_READ_TOKEN }}
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: candy-cloud -> target
      - run: cargo run -p cloud-db --bin cloud-db-migrate
      - run: cargo test --workspace
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: tests/compose_smoke.sh
      - run: docker compose --env-file .env.example config >/dev/null
